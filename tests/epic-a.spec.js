import AxeBuilder from "@axe-core/playwright"; import { expect, test } from "@playwright/test";
test("critical path is keyboard operable and has no serious axe violations", async ({ page }) => { await page.goto("/"); await expect(page.getByRole("heading",{name:/explore|explorar|استكشاف/i})).toBeVisible(); let results=await new AxeBuilder({page}).analyze(); expect(results.violations.filter(v=>["critical","serious"].includes(v.impact))).toEqual([]); await page.getByLabel("Interest").fill("Morocco"); await page.getByRole("button",{name:"Continue"}).click(); await page.getByRole("button",{name:/Build feed/}).click(); await expect(page.locator(".feed-card").first()).toBeInViewport(); results=await new AxeBuilder({page}).analyze(); expect(results.violations.filter(v=>["critical","serious"].includes(v.impact))).toEqual([]); });
test("RTL mirrors controls and persists locale", async ({ page }) => { await page.goto("/"); await page.getByLabel("Language").selectOption("ar"); await expect(page.locator("html")).toHaveAttribute("dir","rtl"); await page.reload(); await expect(page.locator("html")).toHaveAttribute("dir","rtl"); });
test("dark theme and 200 percent zoom remain usable", async ({ page }) => { await page.goto("/"); await page.getByRole("button",{name:"Change theme"}).click(); await expect(page.locator("html")).toHaveAttribute("data-theme","dark"); await page.evaluate(()=>document.body.style.zoom="2"); await expect(page.getByRole("textbox")).toBeVisible(); });
test("focus returns after stepping back", async ({ page }) => { await page.goto("/"); const input=page.getByLabel("Interest"); await input.fill("Morocco"); await page.getByRole("button",{name:"Continue"}).click(); await page.getByRole("button",{name:"Back"}).click(); await expect(input).toBeFocused(); });
